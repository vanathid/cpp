#include <stdio.h>
#include<string.h>
int main(void) {
char sstring[10];
int num,len,i=0;
scanf("%s",sstring);
printf("\nenter number");
scanf("%d",&num);
len=strlen(sstring);
printf("\n");
for(i=len-num;i<len;i++)
{
printf("%c",sstring[i]);	
}
	return 0;
}
